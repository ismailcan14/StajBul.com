import './bootstrap';

// Basit fade-in animasyonu sınıfını eklemek için (sayfa yüklenince)
document.addEventListener("DOMContentLoaded", function () {
    const box = document.querySelector('.login-box');
    box.classList.add('animate');
});

